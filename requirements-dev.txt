# Core framework
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
aiofiles>=23.2

# Database — SQLite driver is built into Python, no extra driver needed
sqlalchemy>=2.0

# Validation & settings
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0

# Authentication
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# Scheduler
apscheduler>=3.10

# Email
aiosmtplib>=3.0
jinja2>=3.1

# HTTP client (for Ollama / Open Food Facts)
httpx>=0.25

# Image processing
Pillow>=10.0
numpy>=1.24

# OCR (Tesseract only — PaddleOCR not supported on Windows)
pytesseract>=0.3.10
opencv-python-headless>=4.8

# Machine Learning
scikit-learn>=1.3
pandas>=2.1
joblib>=1.3

# Forecasting — Prophet requires cmdstanpy; install separately if needed
# prophet>=1.1.5

# NLP / Embeddings — large install; comment out if you want faster setup
sentence-transformers>=2.2
faiss-cpu>=1.7

# Utilities
python-dateutil>=2.8
